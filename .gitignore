# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo

# Environment / secrets
.env
.env.*
!.env.example

# Node / Bun
node_modules/
bun.lockb

# Claude Code local state (session state, local overrides 등은 커밋하지 않음)
# skills/와 agents/는 harness symlink이므로 추적
.claude/*
!.claude/skills/
!.claude/agents/

# git worktree 체크아웃 디렉토리 (install-harness.sh가 생성)
worktrees/
