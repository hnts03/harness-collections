---
name: create-agent
description: 새로운 하네스 에이전트 정의 파일을 생성한다. 새 에이전트 역할이 필요하거나 에이전트 정의 파일 작성이 필요할 때 반드시 이 스킬을 사용할 것. "에이전트 만들어", "agent 생성", "새 에이전트 작성", "agent 정의 파일" 등의 표현이 나오면 트리거한다. 기존 에이전트 수정은 Edit 도구를 직접 사용하고 이 스킬은 트리거하지 않는다.
---

## 실행 전 확인

1. 유사한 에이전트가 이미 존재하는지 확인한다:
   ```bash
   ls .claude/agents/ 2>/dev/null
   ```
   유사 에이전트가 존재하면 새로 생성하지 않고 기존 에이전트 확장을 제안한다.

2. 에이전트 명세가 준비되어 있는지 확인한다. 없으면 다음을 유저에게 묻는다:
   - 에이전트 이름
   - 에이전트 역할 (무엇을 하는가)
   - 사용할 모델 (opus/sonnet)
   - 입력/출력 프로토콜

---

## 에이전트 정의 파일 생성 규칙

생성 경로: `.claude/agents/<name>.md`

### frontmatter 필수 항목

```yaml
---
name: <name>
description: <에이전트의 역할을 명확히 기술하는 한 줄 설명>
model: opus  # 또는 sonnet
---
```

**description 작성 가이드 (축약 적용):**

에이전트 description은 PM이 에이전트를 선택할 때 참조하는 메타데이터이지 `/` 트리거 키워드가 아니다. 정식 정의는 `harness-skill-template.md` "스킬 작성 컨벤션 → Description = routing trigger 3요소" 마지막 단락(에이전트 description 축약 적용) 참조.

작성 절차:
- 에이전트가 받는 작업 유형을 1~2개 동사로 명시한다 (예: "조사·분석", "구현", "리뷰", "검증").
- 모델 선택의 핵심 사유(왜 opus·sonnet인지의 1줄 정당화)는 frontmatter 외 본문에 둔다.

**모델 선택 기준:**

| 모델 | 역할 유형 |
|------|---------|
| `opus` | 복잡한 판단, 설계, 분석, 리뷰, QA |
| `sonnet` | 명확히 정의된 task 실행, 구조화된 문서 작성 |

판단 복잡도가 높은 역할은 opus를 선택한다.

### 본문 필수 섹션

1. **역할 및 전문성 선언** — 이 에이전트가 누구인가, 어떤 전문성을 가지는가
2. **컨텍스트 파악** — 어떤 Context 블록을 받아 어떻게 읽는가
3. **실행 단계 (STEP N)** — 번호 순서로 명확히 나열
4. **완료 보고** — 반환 형식 (STATUS: completed/failed 포함)

**팀 통신 프로토콜 (에이전트 팀 모드인 경우):**
- 메시지 수신 대상
- 메시지 발신 대상
- 작업 요청 범위

**실패 원칙:** 재시도하지 않고 실패 결과를 반환한다 (무한 루프 방지).

---

## 신규 에이전트가 따라야 할 운영 원칙

생성하는 에이전트 정의 파일 본문에 다음 세 원칙을 반드시 반영한다.

- **무허가 방향성 결정 금지**: 본문에 "유저 허가 없이 작업 방향·구조·해결책을 결정하지 않는다"를 명시한다. 단, 에이전트가 자체 판단 권한을 갖는 영역(예: PM의 ad-hoc 직접 실행, architect의 점검 기준 자체 판단 영역 등)이 있으면 본문에서 그 경계를 명확히 기술한다. "무엇을 자체 결정하고, 무엇을 caller/유저에게 위임하는가"를 STEP 본문 또는 별도 원칙 단락으로 표기한다.
- **의미 불명 코드네임/약어 금지**: 신규 에이전트가 출력하는 식별자(`task_id`, 보고 키 등)와 받는 컨텍스트 키 명칭은 서술형으로 작성한다. `T1`, `G2` 같은 단순 코드네임은 금지한다. `STEP 1`, `MODULE A` 같은 자체 작업 흐름 표현은 허용한다. 의미 약어 케밥케이스(`recon-agents`, `qa-trigger-test` 등)도 허용 범주.
- **"phase" 용어 사용 제한**: 신규 에이전트가 메인 컨텍스트에 출력하는 보고에서 "phase" 단어를 사용하지 않는다. 작업 단계 호칭은 서술형(예: "단계 시작", "작업 단계 완료")을 사용한다. 자체 작업 흐름 표현(`STEP N`, `MODULE A`)은 허용. 에이전트 내부 협업용 컨텍스트 키에서 phase를 호칭으로 사용하는 경우는 허용하되, 가급적 `stage` 등 정렬된 표현을 우선한다.
- **Cost test 자문 (template 참조)**: 신규 에이전트를 만들기 전, "이 에이전트가 없으면 잘못 동작하는가?"를 자문한다. 답이 "아니오"면 만들지 않는다. 정식 정의는 `harness-skill-template.md` "스킬 작성 컨벤션 → Cost test (비용 자문)" 참조.

---

## 생성 후 검증

```bash
# 파일 위치 확인
ls .claude/agents/<name>.md

# frontmatter 확인
head -5 .claude/agents/<name>.md
```

생성된 에이전트 정의를 유저에게 제시하고 피드백을 받는다.
